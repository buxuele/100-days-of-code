import time
import math

# zfill

nums = []

print(str(1).zfill(4))      # 0001

import shutil

import os

print(os.getenv("USERNAME"))

import requests

r = requests.get("ok")

print(r.iter_content(chunk_size=1024*1024))
print(r.content)